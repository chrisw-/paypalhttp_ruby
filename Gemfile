source "https://rubygems.org"

gem "rspec"
gem "webmock"
gem "rake"

# Required by Ruby 3
gem 'rexml'

# This had to be removed since it was fixing json gem to a version that is incompatible with Ruby 3
# It is only used to automate the build process and isn't required for the gem to work
# gem "releasinator", '0.7.6'